// TODO: Implement — Admin repair detail (edit, assign, status)
export default function AdminRequestDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  return <div>Admin Request Detail — TODO</div>;
}
